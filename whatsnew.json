{
  "current": "1.108.61",
  "entries": [
    {
      "version": "1.108.61",
      "date": "2026-06-18",
      "title": "Every config key is now documented (no more \"Other\")",
      "summary": "- **13 config keys shipped without a template section or description**, so `config --json` (and any consumer of it, including the jMunch Console's grouped config screen) emitted them under the `Other` catch-all group with an empty `description`. The keys existed in `DEFAULTS`/`CONFIG_TYPES` but were never"
    },
    {
      "version": "1.108.60",
      "date": "2026-06-18",
      "title": "`config --check` distinguishes sandbox-limited storage probes",
      "summary": "- **`config --check` no longer reports a sandbox-limited index-storage probe as a confirmed failure** (issue #335). The storage check creates the index root, writes a `.jcm_probe` file, and unlinks it. Previously *every* exception was treated as a confirmed `storage` issue and exited `1` \u2014 so inside a restricted/sandboxed agent"
    },
    {
      "version": "1.108.59",
      "date": "2026-06-17",
      "title": "Decision-context surfacing in impact analysis",
      "summary": "- **Read-only decision-context surfacing on `get_blast_radius` and `get_impact_preview`** (new opt-in `include_decisions`, default `false`). Impact analysis tells you *what* breaks; this adds *why the code is the way it is*. New `tools/decision_context.py` (`resolve_decision_context`) mines the git history of"
    }
  ]
}
